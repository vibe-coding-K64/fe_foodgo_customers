import 'package:flutter/material.dart';

/// Man hinh dang nhap.
class LoginView extends StatelessWidget {
  const LoginView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Man hinh dang nhap')),
    );
  }
}
